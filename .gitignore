# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.egg
*.egg-info/
.eggs/
build/
dist/
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.ipynb_checkpoints/

# ---- Virtual environments ----
.venv/
venv/
env/
ENV/

# ---- SYNAPSE data (downloaded CWRU / NASA datasets — never committed) ----
data/

# ---- Scenario event logs (regenerated by scripts/run_scenario.py; dashboard consumes them) ----
events/

# ---- Graphify output (machine-generated code/doc graph + Obsidian vault) ----
graphify-out/

# ---- Obsidian (commit vault config, ignore per-machine workspace) ----
.obsidian/workspace*

# ---- Vivarium CAD model (GrabCAD CNC 3018: license/attribution not yet cleared — do NOT commit
#      until verified; the vendored GLTFLoader + hybrid code ARE safe to commit, the .glb is not) ----
dashboard/static/vivarium/models/

# ---- macOS ----
.DS_Store

# ---- Claude Code (local, per-machine settings) ----
.claude/
